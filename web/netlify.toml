[build]
  base    = "web"
  # Site legado apenas mantém redirect permanente para o novo domínio.
  command = "printf 'Legacy redirect → lifeflourishconsulting.com\\n'"
  publish = "netlify/decommissioned"
